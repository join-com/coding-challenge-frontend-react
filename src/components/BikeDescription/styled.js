import styled from 'styled-components'

const Title = styled.h1`
  font-size: 44px;
`

const Label = styled.span`
  font-weight: bold;
`

const MapContainer = styled.div`
  height: 400px;
`

const MapContent = styled.div`
  height: 100%
`

export { Title, Label, MapContainer, MapContent }
